(() => {
  'use strict';

  const STORAGE_KEY='skunkieAssistantChatV2',SESSION_KEY='skunkieSessionId';
  const QUICK_PROMPTS=['What is this portal?','Explain this question','Summarise my progress','What should the pilot include?','Explain the architecture','What are the main risks?','How does WhatsApp work?','What happens next?'];
  const state={open:false,unread:0,busy:false,idleTimer:null,webhookUrl:'',pointer:{x:0,y:0},raf:0};
  const escapeHtml=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[char]));
  const now=()=>new Date().toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});
  const getSessionId=()=>{let value=sessionStorage.getItem(SESSION_KEY);if(!value){value=crypto.randomUUID?crypto.randomUUID():`sk-${Date.now()}-${Math.random().toString(16).slice(2)}`;sessionStorage.setItem(SESSION_KEY,value)}return value};
  const loadMessages=()=>{try{const data=JSON.parse(localStorage.getItem(STORAGE_KEY)||'[]');return Array.isArray(data)?data.slice(-100):[]}catch(_){localStorage.removeItem(STORAGE_KEY);return[]}};
  const messages=loadMessages();
  const saveMessages=()=>{try{localStorage.setItem(STORAGE_KEY,JSON.stringify(messages.slice(-100)))}catch(_){}};

  function avatars(){return [...document.querySelectorAll('.brand-avatar,.wizard-avatar,.skunkie-launcher img,.skunkie-chat-head img')]}
  function setMood(mood,duration=1200){avatars().forEach(image=>{image.classList.add('skunkie-motion');image.dataset.mood=mood});clearTimeout(setMood.timer);if(!['thinking','sleeping'].includes(mood))setMood.timer=setTimeout(()=>setMood('idle',0),duration)}

  function applyPointer3d(){state.raf=0;if(matchMedia('(pointer:coarse)').matches||matchMedia('(prefers-reduced-motion:reduce)').matches)return;const cx=innerWidth/2,cy=innerHeight/2;avatars().forEach(image=>{const rect=image.getBoundingClientRect(),ix=rect.left+rect.width/2,iy=rect.top+rect.height/2,dx=(state.pointer.x-ix)/Math.max(innerWidth,1),dy=(state.pointer.y-iy)/Math.max(innerHeight,1);const proximity=Math.max(0,1-Math.hypot(state.pointer.x-ix,state.pointer.y-iy)/700);image.style.setProperty('--ry',`${Math.max(-12,Math.min(12,dx*28))*proximity}deg`);image.style.setProperty('--rx',`${Math.max(-10,Math.min(10,-dy*24))*proximity}deg`);image.style.setProperty('--tx',`${Math.max(-8,Math.min(8,(state.pointer.x-cx)/cx*7))*proximity}px`);image.style.setProperty('--ty',`${Math.max(-6,Math.min(6,(state.pointer.y-cy)/cy*5))*proximity}px`);image.style.setProperty('--depth',`${proximity*10}px`)})}
  function trackPointer(event){state.pointer.x=event.clientX;state.pointer.y=event.clientY;if(!state.raf)state.raf=requestAnimationFrame(applyPointer3d)}
  function resetPointer(){avatars().forEach(image=>['--rx','--ry','--tx','--ty','--depth'].forEach(name=>image.style.removeProperty(name)))}

  function buildUi(){
    const launcher=document.createElement('button');launcher.className='skunkie-launcher';launcher.type='button';launcher.setAttribute('aria-label','Chat with Skunkie');launcher.setAttribute('aria-expanded','false');launcher.innerHTML='<span class="pulse"></span><img src="assets/skunkie.svg" alt=""><span class="skunkie-badge" aria-label="Unread messages">0</span>';
    const panel=document.createElement('section');panel.className='skunkie-chat';panel.setAttribute('role','dialog');panel.setAttribute('aria-modal','false');panel.setAttribute('aria-label','Chat with Skunkie');panel.innerHTML=`<header class="skunkie-chat-head"><img src="assets/skunkie.svg" alt=""><div><div class="skunkie-chat-title">Aero Discovery Assistant · Skunkie</div><div class="skunkie-chat-status">assessment-aware · repository knowledge enabled</div></div><button class="skunkie-chat-close" type="button" aria-label="Close Skunkie chat">Close</button></header><div class="skunkie-chat-log" aria-live="polite"></div><div class="skunkie-chat-typing">Skunkie is searching and thinking…</div><div class="skunkie-chat-prompts"></div><form class="skunkie-chat-form"><textarea rows="1" maxlength="1000" autocomplete="off" placeholder="Ask Skunkie about the portal, assessment or solution…" aria-label="Message Skunkie"></textarea><button type="submit" aria-label="Send message">➤</button></form><div class="skunkie-chat-foot">Local knowledge works offline. External AI and WhatsApp require a secure webhook.</div>`;
    document.body.append(panel,launcher);
    QUICK_PROMPTS.forEach(prompt=>{const button=document.createElement('button');button.type='button';button.textContent=prompt;button.addEventListener('click',()=>submitMessage(prompt));panel.querySelector('.skunkie-chat-prompts').appendChild(button)});
    launcher.addEventListener('click',togglePanel);panel.querySelector('.skunkie-chat-close').addEventListener('click',()=>togglePanel(false));
    const form=panel.querySelector('form'),input=panel.querySelector('textarea');form.addEventListener('submit',event=>{event.preventDefault();const value=input.value.trim();if(value){input.value='';input.style.height='';submitMessage(value)}});input.addEventListener('input',()=>{input.style.height='auto';input.style.height=`${Math.min(input.scrollHeight,120)}px`});input.addEventListener('keydown',event=>{if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();form.requestSubmit()}});
    if(!messages.length)messages.push({role:'bot',text:'Hello. I’m Skunkie. I can use your assessment context and the repository knowledge store to explain this portal, answer implementation questions, define a pilot, discuss risks, architecture, budget, WhatsApp and next steps.',time:now(),intent:'greeting'});
    renderMessages();return{launcher,panel}
  }

  let ui;
  function renderMessages(){if(!ui)return;const log=ui.panel.querySelector('.skunkie-chat-log');log.innerHTML=messages.map(message=>`<div class="skunkie-chat-message ${message.role==='user'?'user':'bot'}">${escapeHtml(message.text)}${message.sources?.length?`<div class="skunkie-chat-sources">Knowledge: ${message.sources.map(escapeHtml).join(', ')}</div>`:''}<span class="skunkie-chat-meta">${escapeHtml(message.time||'')}${message.intent?` · ${escapeHtml(message.intent)}`:''}</span></div>`).join('');log.scrollTop=log.scrollHeight;saveMessages()}
  function togglePanel(force){state.open=typeof force==='boolean'?force:!state.open;ui.panel.classList.toggle('open',state.open);ui.launcher.setAttribute('aria-expanded',String(state.open));document.body.classList.toggle('skunkie-chat-open',state.open);if(state.open){state.unread=0;updateBadge();setMood('happy');setTimeout(()=>ui.panel.querySelector('textarea').focus(),60)}}
  function updateBadge(){const badge=ui.launcher.querySelector('.skunkie-badge');badge.textContent=String(state.unread);badge.classList.toggle('visible',state.unread>0)}
  function contextSnapshot(){const data=typeof responses==='object'&&responses?responses:{},question=typeof activeQuestions==='function'?activeQuestions()[typeof current==='number'?current:0]:null;return{page:location.pathname,title:document.title,assessment:data,currentQuestion:question?{key:question.key,title:question.title,stage:question.stage,why:question.why}:null,progress:typeof activeQuestions==='function'?{index:(typeof current==='number'?current:0)+1,total:activeQuestions().length}:null,theme:document.documentElement.dataset.theme||'light',knowledgeVersion:window.SkunkieIntents?.version||null}}

  async function callWebhook(message,localResult){if(!state.webhookUrl)return null;const controller=new AbortController(),timeout=setTimeout(()=>controller.abort(),9000);try{const response=await fetch(state.webhookUrl,{method:'POST',headers:{'Content-Type':'application/json','X-Skunkie-Client':'discovery-portal/2.0'},body:JSON.stringify({event:'chat.message',sessionId:getSessionId(),message,localIntent:localResult.intent,localConfidence:localResult.confidence,localSources:localResult.sources||[],context:contextSnapshot(),sentAt:new Date().toISOString()}),signal:controller.signal,credentials:'omit'});if(!response.ok)throw new Error(`Webhook returned ${response.status}`);const data=await response.json();return typeof data.reply==='string'&&data.reply.trim()?{text:data.reply.trim(),intent:data.intent||localResult.intent,sources:Array.isArray(data.sources)?data.sources:localResult.sources}:null}catch(error){console.warn('Skunkie webhook unavailable; using local knowledge response.',error);return null}finally{clearTimeout(timeout)}}
  async function submitMessage(text){if(state.busy)return;state.busy=true;messages.push({role:'user',text,time:now()});renderMessages();setMood('thinking');ui.panel.querySelector('.skunkie-chat-typing').classList.add('visible');try{await window.SkunkieIntents?.ready;const localResult=window.SkunkieIntents?.respond(text)||{intent:'fallback',confidence:.2,text:'I can help with the assessment and repository knowledge.'};const webhookResult=await callWebhook(text,localResult);await new Promise(resolve=>setTimeout(resolve,webhookResult?160:360));const result=webhookResult||localResult;messages.push({role:'bot',text:result.text,time:now(),intent:result.intent,sources:result.sources||[]});renderMessages();setMood(result.intent==='risk'?'alert':'happy');if(!state.open){state.unread+=1;updateBadge()}document.dispatchEvent(new CustomEvent('skunkie:response',{detail:{input:text,...result}}))}finally{ui.panel.querySelector('.skunkie-chat-typing').classList.remove('visible');state.busy=false}}

  function configureWebhook(){const meta=document.querySelector('meta[name="skunkie-webhook"]'),candidate=meta?.content||document.documentElement.dataset.skunkieWebhook||'';try{if(candidate){const url=new URL(candidate,location.href);if(url.protocol==='https:'||(url.protocol==='http:'&&['localhost','127.0.0.1'].includes(url.hostname)))state.webhookUrl=url.href}}catch(_){console.warn('Ignoring invalid Skunkie webhook URL.')}}
  function bindPageReactions(){document.querySelectorAll('.brand-avatar,.wizard-avatar').forEach(image=>{image.classList.add('skunkie-motion');image.dataset.mood='idle';image.addEventListener('mouseenter',()=>setMood('curious'));image.addEventListener('click',()=>{setMood('happy');togglePanel(true)})});document.addEventListener('pointermove',trackPointer,{passive:true});document.documentElement.addEventListener('mouseleave',resetPointer);document.addEventListener('change',event=>{if(event.target.matches('input,textarea'))setMood('happy')});document.getElementById('next')?.addEventListener('click',()=>setMood('happy'));document.getElementById('back')?.addEventListener('click',()=>setMood('curious'));document.getElementById('whyToggle')?.addEventListener('click',()=>setMood('thinking'));window.addEventListener('error',()=>setMood('alert'));['keydown','touchstart','scroll'].forEach(name=>document.addEventListener(name,resetIdle,{passive:true}));resetIdle()}
  function resetIdle(){clearTimeout(state.idleTimer);if(!state.busy)setMood('idle',0);state.idleTimer=setTimeout(()=>{if(!state.open&&!state.busy)setMood('sleeping',0)},30000)}
  function installFavicon(){if(!document.querySelector('link[rel~="icon"]')){const icon=document.createElement('link');icon.rel='icon';icon.type='image/svg+xml';icon.href='assets/favicon.svg';document.head.appendChild(icon)}}
  function init(){installFavicon();configureWebhook();ui=buildUi();bindPageReactions();window.SkunkieAssistant={open:()=>togglePanel(true),close:()=>togglePanel(false),send:submitMessage,setMood,getContext:contextSnapshot,getWebhook:()=>state.webhookUrl,search:query=>window.SkunkieIntents?.search(query)||[]};document.dispatchEvent(new CustomEvent('skunkie:ready',{detail:{intents:window.SkunkieIntents?.names||[],knowledgeCount:window.SkunkieIntents?.knowledgeCount||0,webhookEnabled:Boolean(state.webhookUrl)}}))}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();