(() => {
  'use strict';
  const SPRITE='assets/aero-cgi-sprite.webp';
  const FRAMES={idle:0,wave:1,present:2,thumbs:3,welcome:4,thinking:5,curious:6,excited:7,happy:8,surprised:9,explaining:10,data:11,confident:12,alert:13,concerned:14,success:15,thanks:16,laughing:17,working:18,sleeping:19};
  const SEQUENCES={idle:['idle','curious','idle'],wave:['idle','wave','present','wave'],thinking:['thinking','curious','thinking'],working:['working','data','working'],happy:['happy','success','happy'],alert:['alert','concerned','alert'],sleeping:['sleeping']};
  const image=new Image(); image.decoding='async'; image.src=SPRITE;
  const instances=new Set();
  const dims={cols:5,rows:4,sourceW:1232,sourceH:1277,cropH:282};
  let activeState='idle',pointer={x:innerWidth/2,y:innerHeight/2},raf=0;
  const draw=(instance,state)=>{const index=FRAMES[state]??0,col=index%5,row=Math.floor(index/5),sw=dims.sourceW/dims.cols,sh=dims.sourceH/dims.rows,canvas=instance.querySelector('canvas'),ctx=canvas.getContext('2d');canvas.width=246;canvas.height=282;ctx.clearRect(0,0,canvas.width,canvas.height);ctx.drawImage(image,col*sw,row*sh,sw,dims.cropH,0,0,canvas.width,canvas.height)};
  const animate=instance=>{clearInterval(instance._aeroTimer);const seq=SEQUENCES[instance.dataset.state]||[instance.dataset.state||'idle'];let i=0;draw(instance,seq[0]);if(seq.length>1)instance._aeroTimer=setInterval(()=>draw(instance,seq[++i%seq.length]),360)};
  const make=(source,size='brand')=>{const wrap=document.createElement('span');wrap.className='aero-cgi';wrap.dataset.size=size;wrap.dataset.state=source?.dataset.aeroState||activeState;wrap.setAttribute('role','img');wrap.setAttribute('aria-label',source?.alt||'Aero Discovery Agent');wrap.innerHTML='<canvas aria-hidden="true"></canvas>';source?.replaceWith(wrap);instances.add(wrap);if(image.complete)animate(wrap);else image.addEventListener('load',()=>animate(wrap),{once:true});wrap.addEventListener('click',()=>window.AeroAgent?.open?.());return wrap};
  const upgrade=()=>{document.querySelectorAll('img.brand-avatar,img.wizard-avatar,img.landing-skunkie,img[data-aero-agent]').forEach(img=>{if(img.dataset.aeroUpgraded)return;img.dataset.aeroUpgraded='1';make(img,img.classList.contains('landing-skunkie')?'hero':img.classList.contains('wizard-avatar')?'wizard':'brand')})};
  const setState=(state='idle')=>{activeState=state;instances.forEach(instance=>{instance.dataset.state=state;animate(instance)})};
  const applyPointer=()=>{raf=0;if(matchMedia('(pointer:coarse)').matches||matchMedia('(prefers-reduced-motion:reduce)').matches)return;instances.forEach(instance=>{const r=instance.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2,dx=(pointer.x-cx)/Math.max(innerWidth,1),dy=(pointer.y-cy)/Math.max(innerHeight,1),p=Math.max(0,1-Math.hypot(pointer.x-cx,pointer.y-cy)/720);instance.style.setProperty('--aero-ry',`${Math.max(-14,Math.min(14,dx*34))*p}deg`);instance.style.setProperty('--aero-rx',`${Math.max(-11,Math.min(11,-dy*28))*p}deg`);instance.style.setProperty('--aero-tx',`${Math.max(-9,Math.min(9,dx*22))*p}px`);instance.style.setProperty('--aero-ty',`${Math.max(-7,Math.min(7,dy*18))*p}px`)})};
  document.addEventListener('pointermove',event=>{pointer={x:event.clientX,y:event.clientY};if(!raf)raf=requestAnimationFrame(applyPointer)},{passive:true});
  const observer=new MutationObserver(upgrade); observer.observe(document.documentElement,{childList:true,subtree:true});
  window.AeroCGI={ready:image.decode?.().catch(()=>{})||Promise.resolve(),upgrade,create:make,setState,frames:Object.keys(FRAMES),sprite:SPRITE};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',upgrade,{once:true});else upgrade();
})();